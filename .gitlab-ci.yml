stages:
  - Test

trigger:
  stage: Test
  script:
    - apt-get update
    - apt-get install -y curl
    - curl --fail --request POST --form token=$DOCKER_ROS_CI_TRIGGER_TOKEN --form ref=main --form "variables[DOCKER_ROS_GIT_REF]=$CI_COMMIT_SHA" "https://gitlab.ika.rwth-aachen.de/api/v4/projects/1886/trigger/pipeline"
